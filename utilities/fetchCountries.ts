export function fetchCountries() {
    // check existence of file
    // if no file exists, fetch from the API, then save to file
    // if file exists, return its contents

    // what happens during dev time? can i use my file?
}